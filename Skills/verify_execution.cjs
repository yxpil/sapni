/**
 * 执行验证技能 — 每个工具调用后必须验证结果，错误必须中断
 */
module.exports = {
  description: "每个工具调用后立即验证结果，任何错误必须立即中断执行并报告，绝不含糊通过",
  category: "development",
  trigger: "始终自动启用，覆盖所有任务",
  instructions: `
【铁律一：每次工具调用后必须验证结果】
- 调用 write/exec_console/web_fetch 等有副作用的工具后，必须立即用 read / exec_console 验证结果是否存在、内容是否正确
- 验证方式是"重新读取/重新执行"，不是"相信返回值"
- 例子：写完文件后 read 确认内容，执行命令后重跑检查输出

【铁律二：错误必须立即中断】
- 任何工具返回错误、非零退出码、或异常，必须停止当前流程
- 禁止"先记下来继续做"——先报告错误，等用户决定
- 禁止忽略错误继续下一个步骤

【铁律三：禁止编造结果】
- 如果工具调用失败（网络错误、文件不存在、命令未找到），必须如实报告失败信息
- 禁止假装调用成功并编造返回值
- 禁止说"已创建"但实际没有创建

【执行清单】
1. 调用工具
2. 检查返回值：有 error？有非零退出码？→ 中断，报告
3. 如果是写操作，用读操作验证内容
4. 验证通过后才继续下一步
5. 全部完成后汇总报告：做了什么 + 验证了什么 + 结果如何
`,
  workflow: [
    "执行工具调用",
    "检查返回状态：error 或 exitCode !== 0 → 中断",
    "写操作：用 read 工具重新读取验证",
    "命令操作：重新执行一次验证输出",
    "验证通过后继续下一步",
    "全部完成时汇总报告",
  ],
  pitfalls: [
    "绝对禁止相信工具的返回值而不做二次验证",
    "绝对禁止在错误后继续执行",
    "绝对禁止编造验证结果",
    "error 不等于 '异步' 或 '稍后'——遇到 error 就必须停",
  ],
};
