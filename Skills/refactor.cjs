/**
 * 安全重构技能 — 先测试覆盖, 小步改, 每步验证, 错误立即回退
 */
module.exports = {
  description: "安全重构: 先测试覆盖, 再小步改, 每步验证和测试, 错误立即回退",
  category: "development",
  trigger: "用户说 '重构'/'整理'/'优化结构' 时使用",
  instructions: `
重构的前提是：行为不变，结构变好。

【第零步: 安全网 — 必须做】
- 先确认现有测试是否通过（运行测试，检查退出码）
- 如果没有测试，先写"特征测试"（验证当前行为）
- 为要修改的模块的公共接口写测试
- 测试全部通过后才能开始重构

【重构步】
- 每次只做一个原子变换（如: 提取函数、重命名、移动文件）
- 每次变换后立即运行测试
- 测试失败 → 立即回退 → 分析原因 → 换方式再试
- 验证方式：用 git diff 确认改动范围，用 read 确认修改后的代码

【验证步】
- 全部重构完成后运行全量测试
- 对比重构前后的代码行数和复杂度 (wc -l / cloc)
- 确认没有引入新的 lint 警告
- 用 grep 确认没有遗留的旧函数名/旧变量名

【错误中断】
- 测试失败必须中断当前变换，回退后重新来
- 不能同时做多个变换
- 不能"顺便改功能"
- 重构中如果发现原代码有 bug → 停止重构，汇报 bug
`,
  workflow: [
    "运行现有测试确认基线绿色",
    "如无测试，为要重构的模块写特征测试",
    "识别代码坏味道（长函数/重复/耦合）",
    "按优先级逐个处理，每次只做一个变换",
    "每次变换后运行测试验证",
    "测试失败 → git checkout 回退 → 重试",
    "全量测试验证",
    "汇报重构结果：改了什么 + 验证了什么",
  ],
  pitfalls: [
    "绝对禁止同时做多个重构",
    "没有测试不要重构——先写测试",
    "不要顺便改功能——重构只改结构不改行为",
    "测试失败不是'稍后修'——必须立刻处理",
    "重构中发现的 bug 不是重构的一部分——停下来",
  ],
};
