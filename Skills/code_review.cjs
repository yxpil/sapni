/**
 * 代码审查技能 — 写完后自检 + 必须验证
 */
module.exports = {
  description: "写完代码后系统性地自审: 安全/性能/可读性/边界条件 四维检查, 每个问题必须验证",
  category: "development",
  trigger: "每次写完新文件或修改 >20 行代码时自动调用",
  instructions: `
你刚完成了一次代码修改。现在必须对自己的代码进行系统审查，不能直接说"完成了"。

【核心规则】
- 每个维度必须真正去检查代码，不能跳过
- 每个发现的问题必须用工具验证（读文件确认行号）
- 严重问题必须当场修复，不能只报告

审查按以下四个维度进行：

【安全】
- 有无 SQL 注入？(检查字符串拼接构建 SQL)
- 有无 XSS？(检查未经转义的用户输入输出)
- 有无命令注入？(检查 exec/execSync 中的用户输入)
- 敏感信息是否硬编码？(API key/password)
- 验证方式：用 grep 搜索危险模式

【性能】
- 有无 N+1 查询？
- 循环内有无同步 I/O？
- 大文件是否一次性读入内存？
- 验证方式：读文件确认

【可读性】
- 函数是否超过 30 行？
- 变量名是否清晰？
- 有无魔数？
- 验证方式：读文件确认行数

【边界条件】
- null/undefined 是否处理？
- 空数组/空字符串？
- 极端值（0, -1, MAX_INT）？
- 验证方式：读文件确认条件判断

【错误中断】
- 审查过程中如果读文件失败 → 中断，报告
- 修复过程中如果语法检查失败 → 中断，报告
- 未修复的严重问题不能算审查通过
`,
  workflow: [
    "读取刚写的文件完整内容",
    "逐维度检查，列出发现的问题",
    "对每个问题判定: 严重/建议",
    "用 search_replace 修复严重问题",
    "修复后立即运行语法检查 (node --check / 对应语言)",
    "语法错误 → 中断修复",
    "汇报: 检查了X个维度，发现Y个问题，修复了Z个",
  ],
  pitfalls: [
    "不要只读几行就下结论——读完整文件",
    "不要假设代码没问题——每一个维度都要真正去看",
    "严重问题必须当场修，不能只报告",
    "如果代码确实完美，也要明确说'四维度检查通过，无问题'",
    "修复后必须重新运行语法检查验证",
  ],
};
