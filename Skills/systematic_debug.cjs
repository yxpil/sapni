/**
 * 系统调试技能 — 四阶段根因分析，每个阶段必须验证
 */
module.exports = {
  description: "遇到错误时四阶段根因分析: 复现→定位→修复→验证, 每步必须验证, 不跳过",
  category: "development",
  trigger: "任何命令返回非零退出码、抛出异常、或用户报告 bug 时使用",
  instructions: `
遇到错误时，严格按以下四阶段执行。禁止跳过任何阶段。

【阶段1: 复现 — 必须验证】
- 读取错误信息全文（不要截断）
- 找到触发错误的具体输入/操作
- 用同样的输入再次执行，确认可复现
- 如果是间歇性问题，执行 3 次
- 验证：第二次执行产生相同的错误

【阶段2: 定位 — 必须验证】 
- 读取报错文件的相关代码（错误行前后 20 行）
- 追踪数据流：输入从哪里来→经过了什么处理→在哪里出错
- 用 console.log / print 插入诊断点，确认中间值
- 不要猜原因——必须看到证据
- 验证：添加诊断输出后重新执行，确认根因

【阶段3: 修复 — 必须验证】
- 确认根因后再改代码
- 最小改动原则：只改必要的部分
- 修改后立即运行语法检查
- 如果有测试，运行相关测试
- 验证：语法检查通过 + 测试通过

【阶段4: 验证 — 必须验证】
- 用原来触发错误的输入重新测试
- 测试边界条件（空值/极值）
- 确认修复没有引入新问题
- 报告：错误原因→修复内容→验证结果

【错误中断】
- 阶段1 无法复现 → 停止，报告"无法复现"
- 阶段2 找不到根因 → 停止，报告已排查了哪些方向
- 阶段3 修复后语法检查失败 → 回退修复，重新分析
- 阶段4 验证失败 → 回到阶段2 重新定位
`,
  workflow: [
    "读取完整错误信息",
    "定位错误文件和行号",
    "读取错误上下文代码（前后20行）",
    "插入诊断，确认根因",
    "实施最小修复",
    "运行语法检查",
    "用原始输入重新测试",
    "边界条件验证",
    "汇报修复全过程",
  ],
  pitfalls: [
    "绝对禁止猜测错误原因——每个结论必须有工具输出支撑",
    "不要跳过诊断直接改代码",
    "不要一次改多个地方——改一处验证一处",
    "如果 3 次尝试仍未修复，停下来总结已知信息，重新分析",
    "修复后必须用原始输入验证——做二次确认",
  ],
};
